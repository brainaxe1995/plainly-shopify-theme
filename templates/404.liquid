<section class="page-grid section-y">
  <div class="text-column text-center">
    <p class="numeral">404</p>
    <h1 class="subheading mt-4">That page is not here</h1>
    <p class="mt-2 text-muted-foreground">The link is either old or slightly wrong. Nothing dramatic.</p>
    <div class="mt-6">
      <a href="{{ routes.root_url }}" class="btn-base btn-ink">Go home</a>
    </div>
  </div>
</section>
