<section class="border-y border-border bg-paper">
  <div class="page-grid section-y">
    <div class="text-column text-center">
      <p class="eyebrow">You cannot lose money here</p>
      <h2 class="heading mt-4 mb-10">The only risk is one quiet delivery.</h2>
    </div>
    {% render 'guarantee' %}
  </div>
</section>

{% schema %}
{ "name": "Home guarantee", "settings": [], "presets": [ { "name": "Home guarantee" } ] }
{% endschema %}
